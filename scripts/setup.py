from setuptools import setup, find_packages

setup(
    name="RH2MAS",
    version="0.1",
    packages=find_packages(),
    # If your config.json or other data files need to be included, see MANIFEST.in usage
)
