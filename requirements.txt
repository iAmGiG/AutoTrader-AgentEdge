# AutoGen-TradingSystem Dependencies
# Python 3.12+ recommended for full compatibility

# Core AutoGen 0.7.x framework
autogen-agentchat>=0.7.5
autogen-core>=0.7.5
autogen-ext>=0.7.5

# Trading APIs
alpaca-py>=0.43.2

# Market data sources
polygon-api-client>=1.16.3  # Requires separate Polygon.io API key

# LLM and AI
openai>=2.7.0
tiktoken>=0.12.0

# Data processing
pandas>=2.3.0
numpy>=2.3.0
scipy>=1.16.0
python-dateutil>=2.9.0
pytz>=2025.2

# HTTP clients
requests>=2.32.0
httpx>=0.28.1

# Sentiment analysis (for research/deprecated V0-V4 agents)
nltk>=3.9.0
scikit-learn>=1.7.0

# Utility
python-dotenv>=1.2.0
pydantic>=2.12.0
pyyaml>=6.0.3

# CLI enhancements (Windows tab completion and editing)
# pyreadline3 is pip-only (not available via conda), already installed
# Provides: Tab completion, Ctrl+Backspace, arrow key history on Windows
